# WALK THE STORE

Ohana

Home page
- “Fits with” label on the poster doesn’t add value as we have already filtered so it’s redundant. It is useful on search.
- The chip “on 4 services” is not a good claim for the platforms chip. Let’s change it with “Flatrate”, and the options being “Flatrate”, “free with ads”, “rent”, “buy”, “Any”. Since so far we only track flatrate, keep the others but readonly. This chip also lacks the chevron that makes it obvious that it’s a dropdown. We also need a link to settings in the dropdown pointing to where we can edit our platforms settings.
- How chip color / selected state is used is confused. Dropdowns don’t need to be highlighted (green color),
- The chevron in the dropdown chips is not centered vertically
- Change the “clear” chip with a “close” icon chip
- The “close” icon on tv shows / movies feels redundant as the chip is highlighted when selected.

Movie details
- Make the icons to IMDb, TMDb and FilmAffinity slightly larger (better for mobile tap).
- Sustainability div is redundant with the per-profile chips. Let’s keep only the chips
- Chips must be selectable. When a chip is selected, the next div refreshes calling out where and why a category fits or doesn’t fit.
- The list of categories should have a better design per row in which uses better the horizontal space - right now is 4 rows and no columns . We can probably bring here the progress bars we use in the “parental-guide details” and just merge the two tables.
- The number (1/5) is too small - it’s likely the main thing eyeballs are trying to find and it’s hard to find. Also, we have the decimal number let’s keep it with 1 decimal. The progress bar with colors may help, but think it through. The links to IMDb guide and CSM are useful.
- Where to watch section also has a redundant label (“Available on xxx”) that adds no value vs the chips with the list of actual providers. Let’s just keep the list of providers.

Settings - lists
- The list of lists cx is poor. It’s hard to know where a list starts/end, and know you can click to get to the movies.
